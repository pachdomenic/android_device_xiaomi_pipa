diff --git a/res/values/arrays.xml b/res/values/arrays.xml
index 459e42b..df1b6cd 100644
--- a/res/values/arrays.xml
+++ b/res/values/arrays.xml
@@ -1667,4 +1667,16 @@
         <item>Quick settings</item>
         <item>Lockscreen</item>
     </string-array>
+
+    <string-array name="desktop_pill">
+        <item>Hidden (Transparent)</item>
+        <item>Pill (Default)</item>
+        <item>Three Dots</item>
+    </string-array>
+
+    <string-array name="desktop_pill_values">
+        <item>0</item>
+        <item>1</item>
+        <item>2</item>
+    </string-array>
 </resources>
diff --git a/res/xml/display_settings.xml b/res/xml/display_settings.xml
index 3127d38..aa5cb8d 100644
--- a/res/xml/display_settings.xml
+++ b/res/xml/display_settings.xml
@@ -309,6 +309,15 @@
             android:title="@string/display_vr_pref_title"
             android:fragment="com.android.settings.display.VrDisplayPreferencePicker"/>
             
+        <ListPreference
+            android:key="desktop_mode_pill_style"
+            android:title="Desktop Mode Pill"
+            android:summary="%s"
+            android:entries="@array/desktop_pill"
+            android:entryValues="@array/desktop_pill_values"
+            android:defaultValue="0"
+            settings:controller="com.android.settings.display.DesktopPillPreferenceController"/>
+            
         <!-- Rotation suggestions -->
         <com.android.settings.preferences.SecureSettingSwitchPreference
             android:key="show_rotation_suggestions"
diff --git a/src/com/android/settings/display/DesktopPillPreferenceController.java b/src/com/android/settings/display/DesktopPillPreferenceController.java
new file mode 100644
index 0000000..36ce6e5
--- /dev/null
+++ b/src/com/android/settings/display/DesktopPillPreferenceController.java
@@ -0,0 +1,39 @@
+package com.android.settings.display;
+
+import android.content.Context;
+import android.provider.Settings;
+import androidx.preference.ListPreference;
+import androidx.preference.Preference;
+import androidx.preference.PreferenceScreen;
+import com.android.settings.core.BasePreferenceController;
+
+public class DesktopPillPreferenceController extends BasePreferenceController 
+        implements Preference.OnPreferenceChangeListener {
+
+    private static final String KEY_DESKTOP_PILL = "desktop_mode_pill_style";
+
+    public DesktopPillPreferenceController(Context context, String key) {
+        super(context, key);
+    }
+
+    @Override
+    public int getAvailabilityStatus() {
+        return AVAILABLE;
+    }
+
+    @Override
+    public void updateState(Preference preference) {
+        super.updateState(preference);
+        int value = Settings.Global.getInt(mContext.getContentResolver(), KEY_DESKTOP_PILL, 0);
+        ((ListPreference) preference).setValue(String.valueOf(value));
+        ((ListPreference) preference).setSummary(((ListPreference) preference).getEntry());
+    }
+
+    @Override
+    public boolean onPreferenceChange(Preference preference, Object newValue) {
+        int value = Integer.parseInt((String) newValue);
+        Settings.Global.putInt(mContext.getContentResolver(), KEY_DESKTOP_PILL, value);
+        updateState(preference);
+        return true;
+    }
+}
